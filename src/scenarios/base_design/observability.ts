import { buildCompatScenarioObservability } from '../shared/compatScaffold.ts';
import { compatRuleset } from './content.ts';

export const observability = buildCompatScenarioObservability(compatRuleset);
